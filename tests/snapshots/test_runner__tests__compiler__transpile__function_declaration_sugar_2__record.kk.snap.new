---
source: tests/test_runner.rs
expression: stripped_actual_output.trim()
---
============
INPUT
============
let foo { x: String, y: String = z }: Int =
  x .print;
  z .print;
  123

entry
  foo { x = "Hello", y = "World" }

============
EXIT CODE
============
1

============
STDOUT
============


============
STDERR
============
error:
  ┌─ tests/compiler/transpile/function_declaration_sugar_2/record.kk:1:9
  │
1 │ let foo { x: String, y: String = z }: Int =
  │         ^ Syntax error: not expecting `{` here
  │
  = The expected token here is `:`.

    Note that `{` is used for declaring record type, for example:

    { x: String }

    and constructing record value, for example:

    { x: "hello" }.

    We found this error when we are trying to parse a Let Statement.
    Examples of Let Statement are:

    Example #1:

      let x : Integer = 1

    Example #2:

      let identity = <T>(t: T): T => t
