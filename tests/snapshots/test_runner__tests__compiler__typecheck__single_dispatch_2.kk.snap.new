---
source: tests/test_runner.rs
expression: stripped_actual_output.trim()
---
============
INPUT
============
let foo
  : | String => String
  = | _ => "Hello"

let foo
  : | Boolean => String
  = | _ => "World"

let bar: Integer =
  let f = | x => x.foo
  123

============
EXIT CODE
============
1

============
STDOUT
============


============
STDERR
============
error:
  ┌─ tests/compiler/typecheck/single_dispatch_2.kk:2:5
  │
2 │   : | String => String
  │     ^ Syntax error: not expecting `|` here
  │
  = Note that `|` is only used for creating function, for example:

    | x => x.add(1).

    We found this error when we are trying to parse a Let Statement.
    Examples of Let Statement are:

    Example #1:

      let x : Integer = 1

    Example #2:

      let identity<T> : | T => T = | x => x
